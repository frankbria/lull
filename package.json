{
  "name": "lull",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "description": "Lull — AI self-hypnosis/meditation audio app (working codename)",
  "scripts": {
    "api:dev": "cd apps/api && uv run uvicorn lull_api.main:app --reload",
    "api:test": "cd apps/api && uv run pytest -q",
    "mobile": "pnpm --filter mobile start"
  },
  "engines": {
    "node": ">=20.19.4"
  }
}
